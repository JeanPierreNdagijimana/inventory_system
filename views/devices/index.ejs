<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Device Type</th>
            <th>Device Model</th>
            <th>Device Serial Number</th>
            <th>Device Code</th>
            <th>Device Status</th>
        </tr>
    </thead>
    <tbody>
        <% devices.forEach(function(device) { %>
            <tr>
                <td><%= device.device_type.name %></td>
                <td><%= device.model %></td>
                <td><%= device.serial_number %></td>
                <td><%= device.code %></td>
                <td><%= device.status %></td>
                <td>
                    <form action="/devices/delete/<%= devices.id %>" method="POST" style="display: inline-block;">
                        <input type="hidden" name="_method" value="DELETE">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
            <% }); %>
    </tbody>
</table>
<div>
    <a href="/devices/new" class="btn-primary">Add new Device</a>
    <a href="/dashboard" class="btn-primary">Go back to dashboard</a>
</div>